FROM resin/%%RESIN_MACHINE_NAME%%-openjdk

RUN apt-get update && \
    apt-get install git bluez

RUN git clone git@github.com:intel-iot-devkit/tinyb.git && \
    cd tinyb && \
    mkdir build && \
    cd build && \
    cmake .. && \
    make && \
    make install
